name: Sync Content

# Google Drive sync has been removed. Articles are now managed via
# assets/articles.json directly in the repository.
# This workflow is retained as a placeholder and can be re-enabled
# or repurposed if needed.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-content:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: No-op
        run: echo "Google Drive sync removed. Edit assets/articles.json directly to publish articles."
